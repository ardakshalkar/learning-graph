{
  "name": "d3graph",
  "version": "1.0.0",
  "main": "app.js",
  "dependencies": {
    "graph-data-structure": "^1.12.0",
    "watchify": "^3.11.1"
  },
  "devDependencies": {},
  "scripts": {
    "watch": "watchify app.js -o bundle.js -d",
    "build": "browserify app.js -o bundle.js -d",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
